name: PW Tests

run-name: ${{ github.actor }} is running autotests 🚀  #автор запуска тестов

on: [push, workflow_dispatch]

jobs:
  run-tests:
    runs-on: ubuntu-latest   # запуск автотестов на удаленной виртуальной машине ubuntu
    steps:
       # Шаг 1: Выгружается код, который будет прогоняться при пуше или любом изменении кода
      - name: Check out repository code
        uses: actions/checkout@v4

      # Настроить Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      # Установление зависимостей
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install playwright pytest
          playwright install --with-deps

     #  Вывод списка автотестов
      - name: List Playwright tests
        run: pytest --collect-only

       # Прогон автотестов
      - name: List Playwright tests
        run: pytest --collect-only

      # Загрузка артефактов (отчеты) после выполнения workflow
      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

